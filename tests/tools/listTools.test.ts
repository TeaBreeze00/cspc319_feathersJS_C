// This tool has been removed. See docs/CONTRIBUTOR_PIPELINE_PLAN.md.
// The list_available_tools tool was removed; MCP's native tools/list covers this.
describe('ListToolsTool (removed)', () => {
  it('has been removed in favour of native MCP tools/list', () => {
    expect(true).toBe(true);
  });
});

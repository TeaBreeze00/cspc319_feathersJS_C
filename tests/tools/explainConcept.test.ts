// This tool has been removed. See docs/CONTRIBUTOR_PIPELINE_PLAN.md.
// The explain_concept tool was replaced by search_docs which handles all queries.
describe('ExplainConceptTool (removed)', () => {
  it('has been removed in favour of search_docs', () => {
    expect(true).toBe(true);
  });
});

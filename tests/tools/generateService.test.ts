// This tool has been removed. See docs/CONTRIBUTOR_PIPELINE_PLAN.md.
// The generate_service tool was removed; search_docs handles documentation queries.
describe('GenerateServiceTool (removed)', () => {
  it('has been removed in favour of search_docs', () => {
    expect(true).toBe(true);
  });
});
